image: node:latest

services:
  - mongo

cache:
  paths:
    - code_is_HERE/ttcnpmcrawl/node_modules/

before_script:
    - cd code_is_HERE/ttcnpmcrawl && npm install

all_tests:
  script:
   - nohup npm run start &
   - sleep 3
   - npm run test
